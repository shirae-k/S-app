<div class="main user-show">
  <div class="container">
    <div class="user">
      <h2><%= @user.name %></h2>
      <% if @current_user.id == @user.id %>
      <%= link_to("編集","/users/#{@user.id}/edit")%>
      <% end %>
    </div>
  </div>
</div>
